# CLAUDE.md — reader

**Last updated:** 2026-08-06
**Mode:** Flat

## Overview

Contains 1 source file(s) in `src/reader`.

## File Map

### reader-app.tsx

- **Purpose:** provides `ReaderApp` function; declares `ReaderAppProps` type.
- **Key elements:** `ReaderApp`, `ReaderAppProps`
- **Relations:** Imports `src/domain/reader/types.js`. Imports `src/infrastructure/reader/reader-store.js`. Imports `src/shared/safe-dial.js`. Uses `react`

## Notes / Patterns

<!-- Add folder-specific conventions, gotchas, and tech details here. -->
